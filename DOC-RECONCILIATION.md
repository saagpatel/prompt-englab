# DOC-RECONCILIATION.md

This file was generated by a `/doc-truth-up` documentation-reconciliation pass. The pass treats the
code as read-only ground truth and edits only documentation so it reflects the repo's actual current
state. It changed no source code and ran no builds or tests. This file explains what was checked,
what was found, and what was changed so a reviewer can audit the pass in one read.

---

## Per-Claim Findings

### 1. What it is

**Status:** `consistent`
**Evidence:** `README.md:1-7` intro matches all entry points found in `src/app/` (Next.js App
Router pages: `/`, `/prompts`, `/prompts/[id]`, `/prompts/new`, `/analytics`, `/compare`,
`/settings`). `AGENTS.md` description matches the same codebase. Core modules confirmed:
`src/lib/providers/`, `src/lib/encryption.ts`, `src/lib/templateUtils.ts`.

### 2. Current state

**Status:** `drifted` (AGENTS.md only — see change below)
**Evidence:** `package.json` version `0.1.0`; `CHANGELOG.md` shows v1.0.0 shipped 2026-03-22 with
all four implementation phases (streaming, versioning, test cases, middleware). `src/app/api/`
contains 25 route files covering all advertised features. 4 unit-test files present under
`src/lib/__tests__/` and `src/__tests__/`. `AGENTS.md` "Current State" section was a stale
recovery-pass note claiming streaming/versioning were not yet done — contradicted by code.

**Changed:** `AGENTS.md` "Current State"
> Before: "The repo is active local web-app work. Existing untracked `.perf-results` and `docs/`
> folders are local/generated or parallel-session artifacts, so this recovery pass should only add
> the context file."
>
> After: "v1.0.0 shipped 2026-03-22. All core features are implemented: multi-provider streaming
> (Ollama/OpenAI/Anthropic via SSE), prompt versioning with word-level diffs, template variables,
> named test cases with batch runner, A/B response comparison, cost dashboard, and OCR import. API
> keys are encrypted at rest with AES-256-GCM. GitHub Actions CI runs typecheck, lint, unit tests,
> and build on every push. A production Dockerfile is included. The [Unreleased] section of
> CHANGELOG is empty."

**Changed:** `AGENTS.md` "Next Recommended Move"
> Before: "Add only the context file for this recovery pass, then continue with provider streaming,
> prompt versioning, test-case, and encrypted-key behavior behind normal app verification."
>
> After: "All v1.0.0 features are shipped. The [Unreleased] section of CHANGELOG is empty — no
> planned next work is documented. Expand feature scope (additional providers, prompt sharing, team
> workspaces) or harden the existing surface (integration tests, rate-limit tuning, performance
> profiling)."

### 3. Stack

**Status:** `drifted` (Turbopack claim in README)
**Evidence:** `README.md` tech stack table listed "Next.js (App Router, Turbopack)".
`package.json:8` `dev` script is `"next dev"` with no `--turbopack` flag. `next.config.ts` is an
empty config object with no turbopack settings. Turbopack is not configured or used.

**Changed:** `README.md` tech stack table
> Before: `| Framework | Next.js (App Router, Turbopack) |`
> After:  `| Framework | Next.js (App Router) |`

All other stack entries verified `consistent`:
- Material UI 7 (`@mui/material@^7.3.7`) — `README.md:52`
- Monaco Editor (`@monaco-editor/react@^4.7.0`) — `README.md:53`
- SQLite via Prisma + LibSQL adapter — `prisma/schema.prisma:6-8`, `package.json:27-28`
- OpenAI SDK, Anthropic SDK, Ollama REST — `package.json:18-20`, `src/app/api/ollama/`
- SSE streaming — `src/app/api/anthropic/generate/route.ts:23-77`
- Tesseract.js OCR — `package.json:36`
- Recharts — `package.json:37`
- AES-256-GCM encryption — `src/lib/encryption.ts:3`

### 4. How to run

**Status:** `drifted` (Docker entirely absent from README)
**Evidence:** `Dockerfile` exists at repo root with a full production multi-stage build
(`node:20-alpine`), non-root user, port 3000, `DATABASE_URL=file:./data/prod.db`. CHANGELOG v1.0.0
entry includes "Add Docker support and deployment configuration". README Quick Start contained no
Docker instructions.

**Changed:** `README.md` Quick Start — added Docker subsection after the Build section describing
how to build the image and mount the data volume.

All existing run commands verified `consistent`:
- `npm install` — `package.json` present
- `npx prisma migrate dev` — `prisma/migrations/` contains two migration files
- `npm run dev` — defined in `package.json:9`
- `npm run build && npm start` — both scripts defined in `package.json:11-12`

### 5. Known risks

**Status:** `consistent`
**Evidence:** `AGENTS.md` lists four known risks (encrypted keys, Ollama no-cloud path, streaming
routes, generated artifacts). All are verified by code: encryption in `src/lib/encryption.ts`,
Ollama routes at `src/app/api/ollama/`, SSE streaming in all three provider generate routes.

### 6. Next move

**Status:** `drifted` — AGENTS.md "Next Recommended Move" was stale (addressed under Claim 2).
README has no roadmap section; absence is not a claim, so no edit needed there.

---

## Contradictions for Manual Review

**`docs/execution/evidence-pack.latest.md:8-9`** — Lists three health endpoints that do not exist:
> "Health endpoints checked: /api/live, /api/ready, /api/health"

No routes matching `/api/live`, `/api/ready`, or `/api/health` exist anywhere under `src/app/api/`.
The Dockerfile healthcheck (`Dockerfile:57`) probes `/api/analytics`, which does exist. This
evidence pack is a generated release artifact; it should be regenerated by the release pipeline
rather than edited by hand.

**One-line fix a human should make:** Update `docs/execution/evidence-pack.latest.md` line 9 to
`"Health endpoints checked: /api/analytics"` (or regenerate the file via the release pipeline).

---

## Footer

Generated: 2026-05-30 21:16:49 PDT
Branch: docs/truth-up-2026-05-30
HEAD reconciled: fe7343b70a884d11d6473ebca7e1c586f78d60eb
